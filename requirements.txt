# Versiones fijas: una tesis tiene que poder reproducirse tal cual.
# Instaladas y verificadas el 2026-09-16 con Python 3.13.2 en Windows 11.
#
# pandas queda en la serie 2.2 a proposito: pandas 3 cambio, entre otras cosas,
# la resolucion por defecto de las fechas, y aqui un error de unidades de
# tiempo pasaria en silencio. Ver tests/test_tiempo.py.
pandas==2.2.3
# numpy 2.4.6 es la version mas nueva que convive limpia con pandas 2.2.3:
# desde numpy 2.5.0, `indice + pd.Timedelta(...)` levanta un DeprecationWarning
# desde dentro de pandas. Como los tests tratan esos avisos como error, subir
# numpy a 2.5 rompe la suite a proposito, para que el cambio no pase inadvertido.
numpy==2.4.6
scipy==1.18.1
statsmodels==0.15.0
pyarrow==25.0.1
matplotlib==3.11.2
pytest==9.1.1
tzdata==2026.4
pytz==2026.3.post1   # dependencia de pandas 2.2; se fija para que el entorno sea identico
